(import-macros "testing"
  (test test-compiles is-equal is-thrown includes))
(import "testing/helpers.js" ((alias compile-kernel compile) compile-both))

(test "label: basic"
  (bind result (compile-both "(label my-loop (while true (break my-loop)))"))
  (includes result "myLoop:")
  (includes result "break myLoop"))

(test "label: wrong arity throws"
  (is-thrown (compile "(label foo)") Error))
