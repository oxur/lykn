(import-macros "testing"
  (test test-compiles is-equal is-thrown includes))
(import "testing/helpers.js" ((alias compile-kernel compile) compile-both))

(test "while: basic"
  (bind result (compile-both "(while (> x 0) (-= x 1))"))
  (includes result "while")
  (includes result "x > 0"))

(test "while: missing body throws"
  (is-thrown (compile "(while true)") Error))
